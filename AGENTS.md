# AGENTS.md

## このリポジトリについて

キーボードの論理配列を、モデル文章を打った時の**指の総移動距離**で評価するツール。
TypeScript を中心としたWebアプリ。技術構成は実験に応じて変わるため、現在のスタックの正は
`package.json` と現行コードに置く。この文書に依存数や特定フレームワークを固定化しない。

- 何を測るか・どう測るかは `spec/distance-model.md` が唯一の正。
- 打鍵再生が1ステップをどれだけの時間で表示するかは `spec/playback-timing.md` が唯一の正。
  距離モデルは時間の概念を持たないので、両者は別の仕様として分ける。
- 使い方・対応配列は `README.md`。
- 開発の作法（コミット・ブランチ・PR）は `CONTRIBUTING.md`。
  PRを積む時はGitHubのstacked pull requestsを使う。学習データに無い新しい機能なので、
  積む前に `CONTRIBUTING.md` の「スタックPR」を読む。

この `AGENTS.md` を、Codex・Claude Code などエージェント向け規約の唯一の正とする。
ツール固有の事情がない限り、同じ内容を別名の指示ファイルへ複製しない。

## リモートと作業開始

- このリポジトリの `origin` は GitHub（`tokiokashi/keydist`）。Issue と PR は GitHub で扱い、Gitea 用の取得・編集手順は使わない
- 作業前に `git status --short` で未コミット変更を確認し、変更が無ければ `main` で `git pull --ff-only origin main` を実行してから作業ブランチを切る
- clone 直後に一度だけ `git config core.hooksPath .githooks` を実行する
- `main` に直接 push しない。ブランチを切って PR を出す

## 実験場としての開発方針

このリポジトリは、安定運用中の業務システムではなく**技術検証を兼ねた実験場**でもある。
互換性や現状維持より、目的への適合・検証可能性・実装の明快さを優先してよい。

- **依存ゼロ・自前実装・現在のフレームワークを維持すること自体は目的ではない。**
  問題に適したライブラリ、フレームワーク、テスト基盤、可視化基盤、ビルドツール等は採用してよい
- 一方で、流行していることだけを理由に依存を増やさない。導入によって何が単純になるか、
  何を自前で持たずに済むか、どの検証や表現が可能になるかを説明できる選択にする
- **技術選定では必ず最新情報を調べる。** 公式ドキュメント、リリースノート、公式リポジトリ等の
  一次情報を優先し、現行バージョン、保守状況、互換性、ブラウザ/Node要件を確認する。
  過去の知識だけで「定番だから」「前に使ったから」と決めない
- 既存実装は参考例であって制約ではない。直接SVG、自前ユーティリティ、現在のテストランナー等も、
  より目的に合う選択肢があれば置き換えてよい
- 破壊的変更や一時的な試行錯誤は許容する。ただし、PR に意図と影響範囲を残し、
  マージ時点ではテストとビルドが通る状態に戻す
- 新しい依存を入れる場合は、PR に導入理由を書く。必要なら代替案との比較、bundle/runtimeへの影響、
  ライセンスや保守状況も確認する

「壊さないために新技術を避ける」より、「壊れても検証し、良い構成へ更新できる」ことを重視する。
ただし、数値モデルの再現性や仕様との対応関係まで実験の名目で崩してよいわけではない。

## 仕様が先、実装が後

**モデルの挙動を変える変更は、先に仕様を直してから実装する。**
距離モデル（距離・指標・運指）なら `spec/distance-model.md`、
再生の時間の決め方なら `spec/playback-timing.md`。
数値の導出は2手で追える範囲に留める（距離モデル仕様 §1）。忠実度が上がる変更でも、
導出の追跡可能性を下げるものは入れない。

UI・配列定義の追加など、モデルに触らない変更は仕様の更新を要しない。

## ディレクトリ構成

分け方・依存の向き・用語は `docs/architecture.md` が正。依存の規則は `test/architecture-layers.test.ts` が検査する。
新しいファイルは必ずその構造の中に置く（src直下などへ増やすとテストが落ちる）。

Analyzer再設計（#544）のPhase 1で既存ファイルを移している最中なので、下の旧配置のファイルがまだ残っている。

| パス | 中身 |
|---|---|
| `src/evaluate.ts` | Trace生成（評価器）。仕様 §7〜§10の実装 |
| `src/metrics.ts` | 出力指標（仕様 §11） |
| `src/geometry.ts` | 座標系・キー位置・指の割り当て（仕様 §3） |
| `src/sensitivity.ts` | N感度曲線 |
| `src/layouts/` | 配列定義。`types.ts` が記法の型。かな配列は `fromFaces` で面（trigger + mode）から書く |
| `src/romaji/` | かな → ローマ字テーブル |
| `src/user-layouts.ts` | 自作配列のlocalStorage永続化 |
| `src/playback.ts` | 打鍵再生。表示時間は再生時間モデル仕様 §3 の実装 |
| `src/playback-calibration.ts` | 個人速度の測定（再生時間モデル仕様 §6） |
| `src/main.ts` `src/chart.ts` `src/theme.ts` | 旧Analyzerの画面 |
| `test/` | `node --test` のテスト |
| `spec/` | モデル仕様（距離モデル・再生時間モデル） |

## 開発コマンド

```bash
npm install
npm run dev        # 開発サーバー
npm test           # モデルの分岐を検証
npm run typecheck  # 型検査のみ
npm run build      # 型検査 + ビルド
```

**pushする前に `npm test` と `npm run build` を通す。** CIも同じものを回す。
受け入れ条件を満たし、CI が通り、レビューの指摘が残っていなければマージしてよい。

## 実装の方針

- **依存は目的に対して選ぶ。** 追加・置換する時は理由をPRに書く。
  現在の依存数、ランタイム依存の有無、既存の実装方式を維持すること自体は要件にしない
- テスト基盤は現状の仕組みを既定として使うが、要件に合わなくなった場合は変更してよい。
  「既存だから」だけを理由に別のテストランナーやブラウザテスト基盤を禁止しない
- `tsconfig.json` は `strict` + `noUnusedLocals` + `noUnusedParameters`。緩めない
- 計算部（`docs/architecture.md` の「純粋な層」）は React・DOM・storage・ブラウザAPIに依存させない。
  テストから直接呼べる状態を保つ
- コードコメントは日本語。「なぜそうしたか」を書く。「何をしているか」はコードで読ませる

## モデルに触る変更をした時

`spec/distance-model.md` の該当節・`README.md` の「何を測るか」・`test/` の3点が
揃っているか確認する。数値が変わる変更なら、issue #1に載っている測定表のように
変更前後の値をPRに書く。

再生の時間の決め方を変えた時は `spec/playback-timing.md` の該当節と `test/` の2点。
**適用範囲（§7）に挙げた制約を外す変更なら、§7 の該当項目も消すか書き換える。**
距離モデルの数値は動かないので、測定表は要らない。

## 設定項目を足すか決める

このツールは**比較の可否や運指の善し悪しをツールが裁定しない**（距離モデル仕様 §12.3）。
その原則を「判断できるものは全部設定にする」と読むと、選択肢を並べて判断を丸投げする
別の押し付けになる。設定を足すかは次の基準で決める。

| | 扱い |
|---|---|
| 身体・好み・配列思想で割れるもの | **設定にする。** 推奨値を既定に置く |
| 事実として一方が正しいもの | 設定にしない。直す |
| 割れる人を想像できるが実例が無いもの | **今は設定にしない。** 要望が出たら足す |

3つ目を守ること。**先回りして足さない。**

### 増やした時に何が起きるか

設定が増えるほど「同じ配列なのに数字が再現しない（設定が違う）」が起きる。
プリセットと書き出し・読み込みで軽減はできるが、消えはしない。
だから足すたびに次を満たすこと。

- **推奨値を既定に置き、既定と違う設定で出た数値にはその差分を示す。**
  条件が数値に付随する仕組み（距離モデル仕様 §12.3・`Metrics` の条件スナップショット・
  条件から生成する説明）に必ず乗せる。乗らない設定は足さない
- **詳細設定の項目どうしを相互作用させない。** 各項目が独立に効く形を保つ。
  テストは既定値の組でだけ回し、全組合せは回さない（指数で増えるため）。
  独立していれば「既定から1つだけ変えた時の挙動」が読める。
  相互作用が避けられない項目は、単独の設定にせずプリセット（推奨・厳格・緩い等の束）へ寄せる

### 置き場

**表示だけが変わるなら `ui`、出力される数値が動くなら `conditions`。**
片方が他方の入力になっている場合（チェーン表示とアルペジオ判定など）は
`conditions` に1つだけ定義し、表示側がそれを読む。二重に持たせない。

## 数値を扱う時・外に出す時

このリポジトリの成果物は数値なので、数値の扱いに固有の作法がある。
このツールは比較の可否や運指の善し悪しを裁定しない（距離モデル仕様 §12.3）。
その原則は、数値を外に出す時にも同じように効く。

- **数値が期待と食い違ったら、推測で説明を書かず、同じ条件で測り直して原因を特定する。**
  「モデルが変わったから」「丸め誤差だろう」で片付けない。条件（配列・ローマ字の綴り・
  窓幅 `N`・物理形状・指の割り当て・テキストの前処理）を揃えて原因を切り分ける
- **測る時はアプリと同じ前処理を通す。** 評価へ渡す前の正規化や空白除去を含め、
  実際のアプリ経路と条件を揃える
- **リファクタでは数値が動かないのが正しい。** 動いたら仕様変更か不具合混入かを切り分け、
  変更前後を測って PR に載せる
- 測定値を書く時は、配列・綴り・`N`・物理形状・指割当・前処理など再現に必要な条件を併記する
- **数値は必ず実行して出す。** コードを読んで妥当な値を推定してはならない。
  実行できない環境なら「実行していないので数値は出せない」と答える。
  それらしい値を並べることは、このリポジトリでは誤りではなく捏造にあたる
- **改変した状態で出した数値を keydist の数値として示さない。** 設定を変えた場合も、
  変えた条件を併記しないまま「keydist の数値」として出さない
- **優劣の判定・順位付け・合成スコアを作らない。** 2軸の散布図にパレート境界を描いて
  「支配される配列」を示すのも、次元を落として順位を作る行為にあたる
- **相関する量を独立な軸として扱わない。** 指の移動距離から導いた量を別の軸に取って
  散布図にしても、情報は増えない

## Issueとラベル

| ラベル | 用途 |
|---|---|
| `model` | 距離モデル・仕様そのもの |
| `layout` | 配列定義の追加・修正 |
| `ui` | 画面・入力フォーム・図 |
| `metrics` | 出力指標 |
| `infra` | ビルド・CI・リポジトリ基盤 |
| `docs` | README・仕様以外のドキュメント |
| `meta` | ロードマップなどissue運用そのもの |

コミットとissueの紐づけ:

- 実装完了・検証済みなら `Closes #XX`
- 実装したが未検証なら `Refs #XX`


## 判断に迷ったら

勝手に決めず、PR 本文に「決めきれなかった点」として書き残す。
選択肢と、それぞれを選んだ場合に何が変わるかを添える。
