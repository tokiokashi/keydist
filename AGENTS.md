# AGENTS.md

このリポジトリで作業するエージェント（Codex・Claude Code など）向けの入口。

**まず `CLAUDE.md` と `CONTRIBUTING.md` を読むこと。** 規約の正はそちらにある。
このファイルは、それを読む前に破ると手戻りになる項目だけを先出しする。

keydist は実験場でもある。現在の技術構成や依存ゼロ・自前実装を守ること自体は目的ではない。
目的に合う技術なら新しいライブラリ・フレームワーク・ツールを選定してよい。
ただし「新しいから」だけで導入せず、解く問題と導入効果を説明できること。

## リモートと作業開始

- このリポジトリの `origin` は GitHub（`tokiokashi/keydist`）。Issue と PR は GitHub で扱い、Gitea 用の取得・編集手順は使わない。
- 作業前に `git status --short` で未コミット変更を確認し、変更が無ければ `main` で `git pull --ff-only origin main` を実行してから作業ブランチを切る。

## 破ると手戻りになる 5 項目

1. **依存や既存技術を目的化しない。** 依存追加も自前実装も、それ自体を善悪で決めない。
   目的達成・実装品質・検証速度・保守性に明確な利点があるなら、ライブラリ・フレームワーク・
   テスト基盤・可視化基盤などを追加・置換してよい。技術選定時は最新の公式情報・リリース状況・
   互換性を確認し、PR に「何を解くために、なぜその選択をしたか」を書く。
   「今まで使っていない」「既存コードで自前実装できる」は、それだけでは不採用理由にしない
2. **仕様が先、実装が後。** モデルの挙動を変える変更は `spec/distance-model.md` を、
   再生の時間の決め方を変える変更は `spec/playback-timing.md` を先に直す。
   §11 に節を挿入したら、以降の節番号とコード内の参照コメントを漏れなく振り直す
3. **コミットメッセージは Angular 規約**（`CONTRIBUTING.md`）。commit-msg フックと CI が機械的に検査する。
   clone 直後に `git config core.hooksPath .githooks` を実行する
4. **`main` に直接 push しない。** ブランチを切って PR を出す。
   受け入れ条件を満たし、CI が通り、レビューの指摘が残っていなければマージしてよい
5. **push 前に `npm test` と `npm run build` を通す。** CI も同じものを回す

## 数値を扱う時

このリポジトリの成果物は数値なので、数値の扱いに固有の作法がある。

- **数値が期待と食い違ったら、推測で説明を書かず、同じ条件で測り直して原因を特定する。**
  「モデルが変わったから」「丸め誤差だろう」で片付けない。条件（配列・ローマ字の綴り・
  窓幅 `N`・物理形状・指の割り当て・テキストの前処理）を揃えれば原因は必ず割れる
- **測る時はアプリと同じ前処理を通す。** `src/main.ts` は `SAMPLE_TEXT_JA.replace(/\s+/g, '')`
  してから評価器に渡す。ソースの定数をそのまま使うと文字数がずれる
- **リファクタでは数値が 1 u も動かないのが正しい。** 動いたらリファクタの失敗であって仕様変更ではない。
  変更前後を測って PR に載せる
- 測定値を書く時は条件を併記する。綴りが違うだけで大西配列は 153u と 149u に分かれる
- **数値は必ず実行して出す。** コードを読んで妥当な値を推定してはならない。
  実行できない環境なら「実行していないので数値は出せない」と答える
- **優劣の判定・順位付け・合成スコアを作らない。** 詳細は `CLAUDE.md` の「数値を外に出す時」

## 判断に迷ったら

勝手に決めず、PR 本文に「決めきれなかった点」として書き残す。
選択肢と、それぞれを選んだ場合に何が変わるかを添える。
