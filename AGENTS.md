# AGENTS.md

このリポジトリで作業するエージェント（Codex・Claude Code など）向けの入口。

**まず `CLAUDE.md` と `CONTRIBUTING.md` を読むこと。** 規約の正はそちらにある。
このファイルは、それを読む前に破ると手戻りになる項目だけを先出しする。

## 破ると手戻りになる 5 項目

1. **依存を増やさない。** devDependencies は vite / typescript / @types/node の 3 つだけで、
   ランタイム依存はゼロ。テストランナーは Node 組み込みの `node --test`。
   作図ライブラリも入れない（図は SVG を直接書く。`src/chart.ts` に前例がある）
2. **仕様が先、実装が後。** モデルの挙動を変える変更は `spec/distance-model.md` を先に直す。
   §11 に節を挿入したら、以降の節番号とコード内の参照コメントを漏れなく振り直す
3. **コミットメッセージは Angular 規約**（`CONTRIBUTING.md`）。commit-msg フックと CI が機械的に検査する。
   clone 直後に `git config core.hooksPath .githooks` を実行する
4. **`main` に直接 push しない。** ブランチを切って PR を出す。**merge は人間が行う**
5. **push 前に `npm test` と `npm run build` を通す。** CI も同じものを回す

## 数値を扱う時

このリポジトリの成果物は数値なので、数値の扱いに固有の作法がある。

- **数値が期待と食い違ったら、推測で説明を書かず、同じ条件で測り直して原因を特定する。**
  「モデルが変わったから」「丸め誤差だろう」で片付けない。条件（配列・ローマ字の綴り・
  窓幅 `N`・物理形状・指の割り当て・テキストの前処理）を揃えれば原因は必ず割れる
- **測る時はアプリと同じ前処理を通す。** `src/main.ts` は `SAMPLE_TEXT_JA.replace(/\s+/g, '')`
  してから評価器に渡す。ソースの定数をそのまま使うと文字数がずれる
- **リファクタでは数値が 1 u も動かないのが正しい。** 動いたらリファクタの失敗であって仕様変更ではない。
  変更前後を測って PR に載せる
- 測定値を書く時は条件を併記する。綴りが違うだけで大西配列は 153u と 149u に分かれる

## 判断に迷ったら

勝手に決めず、PR 本文に「決めきれなかった点」として書き残す。
選択肢と、それぞれを選んだ場合に何が変わるかを添える。
